// simple JS object to represent a analysis
// this is used for each of thde positive scale

function analysisScaleObject(pp, up, coeff){
	var positiveScaleType;
	this.possiblePoints = pp;
	this.userPoints = up,
	this.coefficent = coeff;
}
